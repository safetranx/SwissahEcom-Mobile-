import { SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const VendorProfile = () => {
  return (
    <SafeAreaView>
      <Text>VendorProfile</Text>
    </SafeAreaView>
  )
}

export default VendorProfile

const styles = StyleSheet.create({})