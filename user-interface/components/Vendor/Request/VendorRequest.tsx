import { SafeAreaView, StyleSheet, Text, View } from 'react-native'
import React from 'react'

const VendorRequest = () => {
  return (
    <SafeAreaView>
      <Text>VendorRequest</Text>
    </SafeAreaView>
  )
}

export default VendorRequest

const styles = StyleSheet.create({})